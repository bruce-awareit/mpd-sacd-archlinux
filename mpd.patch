diff '--exclude=.git' '--exclude=.github' --unified --recursive --text mpd.orig/src/lib/dvdaiso/meson.build mpd.new/src/lib/dvdaiso/meson.build
--- mpd.orig/src/lib/dvdaiso/meson.build	2026-03-07 15:40:47.000000000 +0800
+++ mpd.new/src/lib/dvdaiso/meson.build	2026-04-02 13:39:15.785708570 +0800
@@ -1,3 +1,9 @@
+# 取得目前使用的 C++ 編譯器
+cpp_compiler = meson.get_compiler('cpp')
+
+# 直接從系統路徑尋找名為 'ixml' 的函式庫 (即 libixml.so)
+ixml_dep = cpp_compiler.find_library('ixml', required: true)
+
 dvdaiso = static_library(
   'dvdaiso',
   'audio_stream.cpp',
@@ -18,6 +24,7 @@
   include_directories: inc,
   dependencies: [
     gcrypt_dep,
+    ixml_dep,
   ],
 )
 
diff '--exclude=.git' '--exclude=.github' --unified --recursive --text mpd.orig/src/lib/sacdiso/meson.build mpd.new/src/lib/sacdiso/meson.build
--- mpd.orig/src/lib/sacdiso/meson.build	2026-03-07 15:40:47.000000000 +0800
+++ mpd.new/src/lib/sacdiso/meson.build	2026-04-02 13:38:44.195709860 +0800
@@ -1,3 +1,9 @@
+# 取得目前使用的 C++ 編譯器
+cpp_compiler = meson.get_compiler('cpp')
+
+# 直接從系統路徑尋找名為 'ixml' 的函式庫 (即 libixml.so)
+ixml_dep = cpp_compiler.find_library('ixml', required: true)
+
 if is_windows
   # avoid the unused libwinpthread-1.dll dependency on Windows; MPD
   # doesn't use the pthread API on Windows, but this is what Meson
@@ -23,6 +29,7 @@
   dependencies: [
     gcrypt_dep,
     threads_dep,
+    ixml_dep,
   ],
 )
 
